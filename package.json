{
  "name": "showcase-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build", // Generates the static site using output: 'export'
    "start": "next start",
    "lint": "next lint",
    "deploy": "npm run build && gh-pages -d out" // Builds and deploys to GitHub Pages
  },
  "dependencies": {
    "autoprefixer": "^10.4.20",
    "framer-motion": "^12.0.5",
    "mathjs": "^14.1.0",
    "next": "13.4.19", // Replace with your Next.js version
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-scroll-parallax": "^3.4.5"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0", // For deploying to GitHub Pages
    "postcss": "^8.5.1",
    "tailwindcss": "^3.4.17"
  }
}
